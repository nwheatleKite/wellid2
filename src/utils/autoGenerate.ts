export const autoGenerateWells = (plateSize: number, format: string = "padded"): string[] => {
    return [];
};

export const autoGeneratePlateWells = (
    plateSize: number,
    format: string = "padded",
    plateNames: string[] = []
) => {
    return [];
};

export type LeftPaddingType = number | "auto"

export const autoGeneratePlateNames = (
    plateSize: number,
    format: string = "padded",
    inputWells: string[],
    plateNumberPadding: LeftPaddingType = "auto"
): string[] => {
    //plate-01, plate-02, plate-03
    return [];
};